version: '3'

# =============================================================================
# Cleanup
# =============================================================================

tasks:

  cleanup:all:
    desc: "🧹 Clean caches & temporary files"
    cmds:
      - bash "{{.DOTFILES_BIN}}/cleanup"
